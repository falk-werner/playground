language: minimal

addons:
    apt:
        update: true
        packages:
            - build-essential
            - cmake

env:
    matrix:
        PROJECT=embed-resources

script:
    mkdir -p "$PROJET/build"
    cd "$PROJET/build"
    cmake -DCMAKE_BUILD_TYPE=Debug ..
    make
